import React from 'react';
import { SvgIconProps } from '@mui/material';
import HowToVoteIcon from '@mui/icons-material/HowToVote';

const VoteIcon = (props: SvgIconProps) => {
    return <HowToVoteIcon {...props}/>
}

export default VoteIcon;
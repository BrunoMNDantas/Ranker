import { SvgIconProps } from '@mui/material';
import MilitaryTechIcon from '@mui/icons-material/MilitaryTech';

const TierIcon = (props: SvgIconProps) => {
    return <MilitaryTechIcon {...props}/>
}

export default TierIcon;
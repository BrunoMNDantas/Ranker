import { SvgIconProps } from '@mui/material';
import LinkIcon from '@mui/icons-material/Link';

const AssignmentIcon = (props: SvgIconProps) => {
    return <LinkIcon {...props}/>
}

export default AssignmentIcon;